const config = {
    database: 'nodedb',          //数据库名称
    username: 'wxwmodder',      //账号名称
    password: 'sxmc321',        //密码
    host: 'localhost',          //主机 默认即可
    port: 3306                   //端口 默认
};

module.exports = config;